; basisobjecten
verzameling: something
functie: something
getal: something

; getallen
dt: getal ; seconde
int: getal
norm: getal ; 0 ≤ x ≤ 1
moment: getal
nat: int ; natuurlijk getal (≥ 0)
byte: nat ; nat < 256
kommagetal: getal

τ: getal
π: getal

icode: something → (nat → (nat → letter))

; html
canvas.fontsize: functie, getal → functie
canvas.linewidth: functie, getal → functie
loadimage: (nat → letter) → image

; webGL
vertexshader: (nat → letter) → vshader
vertexshader: (nat → letter) → vshader
fragmentshader: (nat → letter) → fshader
shaderprogram: vshader, fshader → sprog

vertexbuffer: (nat → getal) → vbuffer
texture: lijst letter, int → texture ; url
cubemap: (nat → (nat → letter)), int → cubemap ; urls

uniformbind: sprog, (nat → letter), something → sprog
matrixbind:  sprog, (nat → letter), something → sprog
shaderbind:  sprog, (nat → letter), vbuffer → sprog
texturebind: sprog, (nat → letter), texture, int → sprog
cubemapbind: sprog, (nat → letter), cubemap, int → sprog
superrender: something, texture, sprog, int → something

; lol/lijst
grab: lijst, int → something
split: lijst, something → lijst
append: lijst, something → lijst
prepend: lijst, something → lijst
compose: (nat → functie) → functie

; net
socket: int
tcp.bind: int, int → socket
tcp.accept: socket → lijst socket
tcp.write: socket, lijst byte → socket
tcp.read: socket → lijst byte

random: getal,getal → getal
download: (nat → letter) → (nat → letter)

maybe: bit
(!): int → int
;(×): lijst, lijst → lijst

; verz
verzameling: (→)
lijst: verzameling
tupel: verzameling
zak: verzameling

; webspecifiek
shape: functie
html: lijst(letter) → lijst(letter)
consoleLog: lijst(letter) → bit
text: something → lijst(letter)
;model: lijst(getal), lijst(getal) → something

setcontext: functie
xdraai: getal → (nat → (nat → getal))
ydraai: getal → (nat → (nat → getal))
zdraai: getal → (nat → (nat → getal))
verplaats: getal,getal,getal → (nat → (nat → getal))
schaal: getal,getal,getal → (nat → (nat → getal))

draw: (nat → functie) → functie
paint: functie, (getal,getal,getal) → functie
rgb: (getal, getal, getal) → (getal, getal, getal)

;vierkant: (ℝ,ℝ),ℝ → vorm
;cirkel: (ℝ,ℝ),ℝ → vorm
;ovaal: ((ℝ,ℝ), ℝ, ℝ, ℝ) → functie
;boog: ((ℝ,ℝ),ℝ,ℝ,ℝ) → vorm
;rechthoek: (ℝ,ℝ),(ℝ,ℝ) → vorm
line: (nat → getal,getal) → functie
newindex: lijst, nat, something → lijst
newindex2: lijst, nat, something → lijst
;label: something → functie
rotate: vorm → vorm
reduce: lijst,(something,something → something) → something
fold: something,lijst,(something,something → something) → something

requestAnimationFrame: (getal → lijst(letter)) → int
contextVan: lijst(letter) → canvas
invoer.registreer: lijst(letter) → lijst(letter)

; hardware
screen.refreshes: bit
mouse.click: bit
mouse.click.begin: bit
mouse.click.end: bit
mouse.moves: bit
mouse.pos: getal, getal
mouse.x: getal
mouse.y: getal

key.code: int
key.begin: int → bit
key.eind: int → bit

syscall: something → int

stdout.write: lijst(letter) → int
app: something

bit: something
yes: bit
no: bit

; functie
something → something: functie
(_fn) : (something→something),something → something
(_) :  ((something→something), something) → something
zip: lijst, lijst → lijst
zip1: something, something → lijst
rzip1: something, something → lijst
map: lijst, (something→something) → lijst
filter: lijst, (something→bit) → lijst

; lijst
(×) : lijst, lijst → lijst
(‖) :  lijst, lijst → lijst
(∘) : functie, functie → functie
(|) : lijst → something
(=) :  (something, something) → bit 
(:=) : (something, something) → bit
(→) :  (something, something) → (something → something)

sin :  getal → getal
cos :  getal → getal
tan :  getal → getal
sincos :  getal → getal, getal
cossin :  getal → getal, getal
;(+) :  something, something → something
;(+) :  int, int → getal
;(-) :  getal → getal
;(·) :  (getal, getal) → getal
;(/) :  (getal, getal) → getal
;(/) :  (int, int) → getal
;(^) :  (getal, getal) → getal
;(^) :  (int, int) → int
;(^) :  (int, int) → int
;(^) :  ((→), nat) → something
(^): something,getal → something
(#) :  lijst → int
(√) : getal → getal

; set!
set: verzameling
(⋂) : verzameling set → set
(⋃) : verzameling set → set
(∩) : set, set → set
(∪) : set, set → set
;(-) : set, set → set
(∖) : set, set → set

; logica
(¬) : bit → bit
;(') : something → something
;(>) : getal, getal → bit
(≥) : getal, getal → bit
(≤) : getal, getal → bit
;(<) : getal, getal → bit
(∧) : bit, bit → bit
(∨) : bit, bit → bit
(⋀) : (nat → bit) → bit
(⋁) : (nat → bit) → bit

; lijst
(Σ) : lijst getal → getal
;(∫) : (ontelbaar getal) → getal
(..) : int, int → (nat → int)

; util
abs: getal → getal
absd: getal → getal
max: (getal,getal) → getal
min: (getal,getal) → getal
clamp: (getal,getal,getal) → getal
round: getal → int
ceil: getal → int
floor: getal → int
(:): (something,something) → bit

(something → something) : something
(something → int) : something

byte: int
letter: int
tekst: lijst letter

runtime : getal
now : getal
find : (lijst, something, int) → int
until : (lijst, int) → lijst
from : (lijst, int) → lijst
slice : (lijst, int, int) → lijst

;herhaal : ((→) → (→))
;herhaal : ((→), int) → (→)
where : ((nat → something), (something → bit)) → (nat → something)
mod : getal, getal → getal
